import 'package:flutter/material.dart';

class OverflowMenu extends StatelessWidget {
  const OverflowMenu({Key? key, required this.title, required this.onTap})
      : super(key: key);

  final String title;
  final Function onTap;

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
